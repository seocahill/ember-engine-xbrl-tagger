/* eslint-env node */
'use strict';

module.exports = function (environment) {
  const ENV = {
    modulePrefix: 'ember-xbrl-tagger',
    podModulePrefix: 'ember-xbrl-tagger/pods',
    environment: environment
  }

  return ENV;
};